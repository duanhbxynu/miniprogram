<!--pages/wxs/wxs.wxml-->
<!-- 错误的写法 -->
<!-- <view>{{25.66666.toFixed(2)}}</view> -->

<!-- wxs的定义方式 -->

<!-- 方法1：直接定义在页面中 -->
<!-- <wxs module="info">
  //JS代码，不能写es6语法
  var message = "hello world";
  var name = "coderwhy";

  function sum(num1, num2) {
    return num1 + num2
  }

  function test() {
    return "test function"
  }

  //commonjs的模块化导出的写法
  module.exports = {
    message: message,
    name: name,
    sum: sum
  }
</wxs> -->

<!-- 方法2：外部导入wxs 使用wxs标签进行导入 不能使用绝对路径 要使用相对路径 -->
<wxs src="../../wxs/info.wxs" module="info" />

<view> {{info.message}} </view>
<view> {{info.name}} </view>
<view> {{info.sum(20,30)}} </view>

<!-- 3.wxs的应用 -->
<wxs src="../../wxs/format.wxs" module="format" />
<view>{{format.priceFormat(price,3)}}</view>